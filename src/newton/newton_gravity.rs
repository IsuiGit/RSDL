use crate::newton::Newton;
use crate::collider::Collider;
use std::time::{SystemTime, UNIX_EPOCH};

impl Newton{
    pub fn uniformly_accelerated_motion(&self, playable: &mut Collider){
        
    }
}
